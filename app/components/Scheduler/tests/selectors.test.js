// import { selectScheduleTimingsDomain } from '../selectors';

describe('selectScheduleTimingsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
