// import { selectBookingSuccessDomain } from '../selectors';

describe('selectBookingSuccessDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
