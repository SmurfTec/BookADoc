// import { selectUserAppointmentsDomain } from '../selectors';

describe('selectUserAppointmentsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
