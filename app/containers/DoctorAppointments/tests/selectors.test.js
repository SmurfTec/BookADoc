// import { selectPatientAppointmentsDomain } from '../selectors';

describe('selectPatientAppointmentsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
